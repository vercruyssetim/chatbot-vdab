import {Answer} from "./answer";
describe('answer', () => {
    beforeEach(() => {

    });

    it('', () => {
        let answer = new Answer('conversationtype','type','value');
        answer.getConversationType();
    });
});
